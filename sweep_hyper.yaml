method: bayes
metric:
  goal: maximize
  name: test/accuracy
parameters:
  ^chance_constraint:
    distribution: uniform
    min: 0.
    max: 0.01
  ^temperature:
    distribution: uniform
    min: 0.01
    max: 10.
program: train.py
